{
  "name": "noitis-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node scripts/ensure-local-env.mjs && vite",
    "build": "node scripts/generate-publication-files.mjs && tsc -b && vite build",
    "preview": "vite preview",
    "check:scripts": "node --check scripts/browser-smoke.mjs && node --check scripts/phase6-live-smoke.mjs && node --check scripts/verify-publication-health.mjs && node --check scripts/phase7-compatibility.mjs",
    "check:content": "node scripts/verify-content.mjs",
    "check:links": "node scripts/verify-links.mjs",
    "check:quality": "node scripts/verify-quality.mjs",
    "check:browser": "node scripts/browser-smoke.mjs",
    "check:browser:live": "node scripts/phase6-live-smoke.mjs",
    "check:production": "node scripts/verify-production-config.mjs",
    "check:live": "node scripts/verify-publication-health.mjs",
    "check:phase6": "npm run check:production && npm run check && npm run check:live && npm run check:browser:live",
    "check:phase7:compat": "node scripts/phase7-compatibility.mjs",
    "check:phase7:public": "node scripts/phase7-compatibility.mjs --url=https://noitis-mc.github.io/noitis-website/",
    "check:phase7": "npm run check && npm run check:browser && npm run check:phase7:compat",
    "check:phase7:release": "npm run check:phase6 && npm run check:phase7:public",
    "check": "npm run check:scripts && npm run build && npm run check:content && npm run check:links && npm run check:quality"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-icons": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest"
  }
}
